FireUp is a base for Firefox 3.6.*/4.0b8pre-compatible extensions. Contained
within are the established directory and file structure, with top-level
directories for XPCOM components ("components"), JavaScript code modules
("modules"), and plugins ("plugins"); basic preference and locale data.
Overlay script ensures extension startup, initialization of preference
branch access and string bundle access, shutdown.

MIT license is used throughout the code.

Developed by Dmitriy Khudorozhkov (dmitriy.khudorozhkov@gmail.com).